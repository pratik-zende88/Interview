apiversion: v1
kind : Pod
metadata:
  name: my-pod
  lables:
    app: my-app
    type: frontend
    environment: dev
spec:
  containers:
  - name: nginx-container
    image: nginx:latest
    portd:
    - containerPort: 80




# kubectl apply -f pod.yml
# kubectl get pods
# kubectl get pods -o wide
# kubectl describe pod my-pod
# kubectl logs my-pod